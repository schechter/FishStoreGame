%h1
  pay game mode
-fish = @fish.first

= link_to "sell #{fish.name} the fish", sell_me_path(id: fish.id), method: :post, class: 'button'

/ = link_to 'Purchase this Album', purchase_path(id: @album.id), method: :post, class: "button"